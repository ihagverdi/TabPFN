`SAMPLE_SUBSAMPLING_METHOD="majority_downsample"` now corrects the target prior shift it introduces, so predicted probabilities and regression distributions are calibrated to the training data rather than to the downsampled context. `predict_proba_batched` and `predict_batched` raise with this sampler; score datasets individually instead.
